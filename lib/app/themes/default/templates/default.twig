{% extends '@default/partials/base.twig' %}

{% block title %}Index{% endblock %}

{% block head %}
    {{ parent() }}
    <style type="text/css">
        .important { color: #336699; }
    </style>
{% endblock %}

{% block content %}
	{% if is_markdown %}
        {{markdown(content)}}
    {% else %}
        {{content}}
    {% endif %}
{% endblock %}
